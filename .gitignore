**/build
**/.DS_Store
**/*.test
**/profile
.direnv

provider/provider
provider_bin
connectctl/connectctl
tetherctl/*.conf
api/redoc-static.html
security/ips.{go,txt}
progress.md
*.bak
.worktrees
docs/superpowers/
.claude/
test_bin
hub/hub
hub/hub_bin
hub.db
prs.json
# Go build artifacts
/urnet-docker
/urnet-docker.exe
/urnet-tools
/urnet-tools.exe
# Any stray cross-compiled binary. `go build ./provider/` writes provider.exe
# under GOOS=windows (it cannot write "provider" -- that path is the source
# directory), and a 40MB artifact is easy to sweep in with `git add -A`.
# Deliberately not "/provider": that would ignore the source tree.
*.exe
review-egg-dsflash-v2.md
/dist/
