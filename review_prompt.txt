You are doing a pre-merge code review of branch fix/ci-smoke-lifecycle in this repo (urnetwork-3.23-fix, a Go provider fork). The branch has ONE commit (HEAD) on top of origin/main (f4b40eb7). Review the FULL diff: `git diff origin/main...HEAD`. Do NOT modify any files under review. Write your findings to a NEW file: /tmp/ci-fix-work/sonnet-review.md

## What the PR does

Fixes three CI workflows red on main since the lifecycle merge, plus teardown hardening and an upstream-monitor guard:

1. Stopped-provider discovery: Discover() dropped F2 wiring (attachUnits + discoverSystemdUnits) in an earlier commit. Restored via per-platform discoverStopped hook (linux: attachUnits + unit scan; darwin/windows: no-op). providerCandidateUsers returns usernames not home paths. discoverUserUnits uses plain `systemctl --user` for the current user (the -M form fails on CI runners via machined). BOTH systemctl list-units calls now use --plain (the bullet "●" column was parsed as fields[0], so loaded-failed units were never matched — the fake unit was installed but undiscoverable).
2. Timer create/remove: writeTimerCalendar now CREATES a missing timer file (atomic temp+rename, mirroring the shell installer template); auto-update off removes the timer file. writeTimerUnitAtomic is in the SHARED lifecycle_cmds.go (pure file I/O) so Windows tests build.
3. Docker CLI: consumeDockerBareTarget promotes a bare positional matching a discovered container to the target (status/logs/restart/proxy). proxy add passes --proxy_file= so the in-container shell wrapper reads the file.
4. Workflow teardowns: all provider-starting workflows do SIGTERM + state-dir/config-volume wipe in if: always() so cancelled runs don't leave phantom providers / client JWTs on the testing account.
5. upstream_monitor: guard compare jq against null (out-of-order tags crashed monitor-build-releases).

Files changed: internal/urnettools/{discover.go, discover_unix.go, discover_darwin.go, discover_windows.go, lifecycle_unix.go, lifecycle_cmds.go, cli_docker.go, lifecycle_test.go, review_fix_test.go}; .github/workflows/{unix-lifecycle.yml, tool-functional-smoke.yml, tool-functional-soak.yml, docker-functional-soak.yml, docker-multi-container.yml, upstream_monitor.yml}

## Focus areas

- Concurrency/correctness of the discovery restore (attachUnits before unit scan so unitIn dedup works; current-user vs -M branch)
- writeTimerCalendar create path vs the shell installer template; atomicity of writeTimerUnitAtomic; off-path removal semantics (disable error returned even when file removed — defensible?)
- consumeDockerBareTarget: ambiguity between a bare container name and a proxy file path/URL; flag-skipping; the proxy add <file> case
- proxy add --proxy_file= through the in-container shell wrapper (urnet-tools.sh) AND the Go tool
- Workflow teardowns: if: always() semantics, rm -rf scoping (only runner-created dirs), docker volume removal
- The unix-lifecycle workflow bootstrap (loginctl enable-linger + daemon-reexec + daemon-reload) and fake-unit creation; the --plain discovery fix
- TestCmdReportWritesOverrideFile skip guard placement
- Any Go style/race/error-handling issue in the new code

## Output format

Write sonnet-review.md with: severity-ranked findings (CRITICAL/HIGH/MEDIUM/LOW/NIT), each with file:line refs verified against the CURRENT branch, why it matters, suggested fix. Then a final VERDICT: APPROVE or REQUEST_CHANGES. Verify claims with git (git show / git log -S) where attribution matters. You may run go build / go vet / go test ./internal/urnettools/ to verify claims but must not modify source files.
