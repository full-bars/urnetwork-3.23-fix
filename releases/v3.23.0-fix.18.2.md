# Release v3.23.0-fix.18.2

**Date:** 2026-06-08  
**Type:** Critical Hotfix  
**Severity:** High

## Critical Fix: Proxy Authentication Failure Causing Provider Crash

> [!WARNING]
> **Critical Issue Resolved:** A single proxy failing authentication during startup would call `os.Exit(1)` and **crash the entire provider**. On large deployments with thousands of proxies, this was catastrophic—any proxy hitting API timeouts during startup could kill the whole host.

### What Was Fixed

**Before:** When a proxy failed authentication 10 times, it called `os.Exit(1)`, killing the entire provider process mid-startup.

**After:** Failed proxies now exit gracefully, allowing the provider to stay up while the failed proxy waits for the hourly pulse retry mechanism.

### Changes in This Release

1. **Graceful proxy auth failure handling** (provider/main.go)
   - Replaced fatal `os.Exit(1)` with graceful goroutine return
   - Failed proxy no longer crashes entire provider
   - Other proxies continue starting normally
   - Provider remains operational and earning

2. **Enhanced logging with retry counters**
   - Added `[proxy][init]` prefix for all proxy auth messages
   - Shows proxy index and address in every message
   - Retry counter shows attempt number (e.g., `attempt 3/10`)
   - Enables debugging which proxy is failing and why

3. **Suppressed "exit idle timeout" log spam** (transfer.go)
   - Demoted sequence idle timeout messages from `Errorf` back to `V(2)`
   - Prevents RAMlog and dashboard flooding on high-volume providers
   - Restores visibility of actual errors by removing non-critical noise

4. **Improved Installer Resilience** (scripts/Provider_Install_Linux.sh)
   - Refactored `network_fetch` and `download_asset` with multi-retry logic (3 attempts)
   - Added exponential backoff and connection-refused retries
   - Fixed fallback logic: now correctly tries `wget` if `curl` fails (e.g., on 504 Gateway Timeout)

### Example Log Messages

**Before (cryptic, no context):**
```
init proxy auth failed: Timeout.. Will retry in 7.80s
```

**After (clear, actionable):**
```
[proxy][init] proxy[3633] (69.3.169.69:1081) auth failed (attempt 3/10): Timeout. Will retry in 7.80s
[proxy][init] proxy[3633] (69.3.169.69:1081) authentication failed after retries: Timeout (proxy will remain offline, retry on next hourly pulse)
```

### Impact

- **Large deployments:** Provider stays up even during API load/timeouts
- **Operations:** Clear logs show which specific proxies are failing and why
- **Reliability:** Failed proxies integrated into existing hourly pulse retry mechanism
- No breaking changes: Full backward compatibility

### Historical Context & Root Cause

> [!TIP]
> This regression was introduced in **v3.23.0-fix.13** (May 27, 2026). Prior to that release, proxies would retry authentication infinitely on timeout; while this avoided the crash, it made it impossible for the provider to recover from actually expired or revoked JWTs without manual intervention.
>
> In `.13`, we introduced a 10-retry limit to allow the shell loop to take over for re-authentication, but the unintended side-effect was that reaching this limit triggered a fatal `os.Exit(1)` inside the per-proxy goroutine. This release (.18.2) preserves the retry limit and recovery path while making the failure graceful.

## Deployment

Deploy immediately to all production servers to prevent provider crashes during high API load.

## Testing

- ✅ Verified single proxy auth failures don't crash provider
- ✅ Confirmed other proxies continue starting unaffected
- ✅ Tested failed proxy retry on next hourly pulse
