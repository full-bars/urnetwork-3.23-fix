### v3.23.0-fix.15.4 — Force Update Flag

### Added

• **`urnet-tools update -f` / `--force`**: Bypasses the version date check and re-downloads and reinstalls even if the installed version matches the available version. Useful when a release tag is re-tagged with updated binaries, or for manual recovery when the local install is known to be corrupt or out of sync.

```sh
urnet-tools update -f
urnet-tools update --force
```

Without `-f`, update exits early with "already up to date" if the version strings match. With `-f`, it always fetches and reinstalls regardless.

### What's Changed
• `feat: add -f/--force flag to urnet-tools update for re-downloading same version` — bdacfa7

**Full Changelog**: https://github.com/full-bars/urnetwork-3.23-fix/compare/v3.23.0-fix.15.3...v3.23.0-fix.15.4
